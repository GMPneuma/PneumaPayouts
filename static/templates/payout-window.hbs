<form class="payout-form" autocomplete="off">
  <header class="payout-heading">
    <p>Select the players and characters who participated in this session.</p>

    <div class="payout-metadata">
      <label>
        <span>Session label</span>
        <input
          type="text"
          name="sessionLabel"
          placeholder="Session name or number"
          required
        />
      </label>

      <label>
        <span>Notes</span>
        <textarea
          name="notes"
          rows="2"
          placeholder="Optional payout notes"
        ></textarea>
      </label>
    </div>
  </header>

  <section class="participant-list" aria-label="Session participants">
    {{#each players}}
      <article
        class="participant {{eligibilityClass}}"
        data-player-row
        data-user-id="{{userId}}"
      >
        <header class="participant-header">
          <label class="participant-toggle">
            <input
              type="checkbox"
              name="userIds"
              value="{{userId}}"
              data-user-toggle
              data-selected="{{selected}}"
              data-eligible="{{eligible}}"
            />
            <strong>{{userName}}</strong>
          </label>

          <span class="presence {{presenceClass}}">
            <i class="fas fa-circle" aria-hidden="true"></i>
            {{presenceLabel}}
          </span>
        </header>

        {{#if actors.length}}
          <div class="actor-options">
            {{#each actors}}
              <label class="actor-option">
                <input
                  type="checkbox"
                  name="actorIds"
                  value="{{actorId}}"
                  data-actor-toggle
                  data-actor-id="{{actorId}}"
                  data-assigned="{{assigned}}"
                  data-selected="{{selected}}"
                />
                <span>{{actorName}}</span>
                {{#if assigned}}<span class="tag">Default</span>{{/if}}
                {{#if shared}}<span
                    class="tag tag--warning"
                  >Shared</span>{{/if}}
              </label>
            {{/each}}
          </div>
        {{else}}
          <p class="empty-actors">No eligible character Actors.</p>
        {{/if}}

        {{#if warnings.length}}
          <ul class="participant-warnings">
            {{#each warnings}}<li>{{this}}</li>{{/each}}
          </ul>
        {{/if}}
      </article>
    {{else}}
      <p class="empty-players">No player accounts exist in this world.</p>
    {{/each}}
  </section>

  <footer class="payout-footer">
    <span data-selection-summary></span>
    <span class="next-step"><i class="fas fa-info-circle"></i>
      Reward fields are added next.</span>
  </footer>
</form>